# JSON Config Sanity Checker

Catches obvious nonsense in your JSON config files before it ruins your debugging session. Run: `node config-sanity.js [files...]` or just `node config-sanity.js` to check common configs.